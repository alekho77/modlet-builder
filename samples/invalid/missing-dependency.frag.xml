<modlet>
  <!--
    INVALID: missing dependency.
    This fragment declares requires="invalid.items.nonexistent" but no
    fragment with that name exists anywhere in the source set.
    Expected error: Fragment 'invalid.recipes' requires 'invalid.items.nonexistent' which is not defined.
  -->
  <fragment name="invalid.recipes" target="recipes" requires="invalid.items.nonexistent">
    <append xpath="/recipes">
      <recipe name="testRecipe" count="1"/>
    </append>
  </fragment>
</modlet>
